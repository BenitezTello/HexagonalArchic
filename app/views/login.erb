<!-- app/views/login.erb -->

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Iniciar Sesión - ABT Zone</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <!-- Font Awesome CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <!-- Estilos Personalizados -->
  <style>
    body {
      background: #f8f9fa; /* Color de fondo suave */
    }
    .login-container {
      margin-top: 50px;
      
   
      min-height: 80vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-form {
      padding: 30px;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 400px;
    }
    .login-form .form-group {
      position: relative;
    }
    .login-form .form-group .fa {
      position: absolute;
      top: 50%;
      left: 15px;
      transform: translateY(-50%);
      color: #6c757d;
    }
    .login-form input {
      padding-left: 40px; /* Espacio para el icono */
    }
    .login-form button {
      width: 100%;
    }
    /* Efectos de Focus */
    .login-form input:focus {
      border-color: #80bdff;
      box-shadow: 0 0 5px rgba(0,123,255,0.5);
    }
    /* Responsive */
    @media (max-width: 576px) {
      .login-form {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="container login-container">
    <div class="login-form">
      
      
      <h3 class="text-center mb-4">Iniciar Sesión</h3>
      
      <form action="/login" method="POST">
  <!-- Mensajes de Error Dentro del Formulario -->
  <% if flash[:error] %>
    <div class="alert alert-danger" role="alert">
      <%= flash[:error] %>
    </div>
  <% end %>
  
  <!-- Campo de Usuario -->
  <div class="form-group">
    <i class="fa fa-user"></i>
    <input type="text" name="username" class="form-control" placeholder="Nombre de Usuario" required aria-label="Nombre de Usuario">
  </div>
  
  <!-- Campo de Contraseña -->
  <div class="form-group">
    <i class="fa fa-lock"></i>
    <input type="password" name="password" class="form-control" placeholder="Contraseña" required aria-label="Contraseña">
  </div>
  
  <!-- Botón de Envío -->
  <button type="submit" class="btn btn-primary">Entrar</button>
</form>
      
      <!-- Enlace a Registro -->
      <p class="mt-3 text-center">
        ¿No tienes una cuenta? <a href="/register">Regístrate aquí</a>
      </p>
    </div>
  </div>

  <!-- Bootstrap JS y dependencias -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
